* default rendering with just a write to the ping pong buffer and then to the screen
* don't create gpu resources per-frame
* make post-processing opt-in
* handle resizing of ping-pong buffers
* check perf on real project
* add a doc for writing a post-process shader
